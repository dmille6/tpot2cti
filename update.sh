#!/usr/bin/env bash
# tpot2cti — pull latest, rebuild, restart our stack
# (use --opencti to also bump OpenCTI to the new pinned version)
set -euo pipefail
echo "tpot2cti update.sh — not yet implemented."
exit 1
