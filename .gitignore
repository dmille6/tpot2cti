# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/

# Environment / secrets
.env
ssh-keys/
!ssh-keys/.gitkeep

# Runtime data (per V1_SPEC §2.4 — gitignored). Anchored to repo root so
# it doesn't accidentally exclude tpot2cti/data/ (which ships the
# benign_scanners.yaml allowlist).
/data/
!/data/.gitkeep

# Logs (per V1_SPEC §2.4 — gitignored)
/logs/
!logs/.gitkeep

# OpenCTI clone (created by setup.sh next to us)
opencti/

# OS / editor
.DS_Store
Thumbs.db
*.swp
*~
.vscode/
.idea/

# Local override files
docker-compose.override.yml
